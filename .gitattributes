* text=auto eol=lf

# Cooked CR2W resources are binary even when the depot extension is `.json`.
examples/**/source/archive/** -text -eol

# Original and packed audio assets must never receive line-ending conversion.
*.wav -text -eol
*.wem -text -eol
